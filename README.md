# Sussex PhD Thesis using LaTeX

- Compiled with `pdfLaTeX`
- Bibliography uses `natbib` + `bibTeX`
- Terms / Acronyms using `glossaries-extra` + `bib2gls`

A few changes from Martin's template will be added at a later date
